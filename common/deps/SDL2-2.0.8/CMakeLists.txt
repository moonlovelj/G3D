set(SDL_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE PATH "" FORCE)
set(SDL_HEADER_DIR ${SDL_ROOT_DIR}/include)
file(GLOB SDL_DLLS ${SDL_ROOT_DIR}/lib/x86/*.dll)

include_directories(${SDL_HEADER_DIR})

add_library(SDL2 SHARED IMPORTED)
add_library(SDL2main SHARED IMPORTED)

if(WIN32)
    set(SDL_LIBRARY_DIRS "${SDL_ROOT_DIR}/lib/x86" CACHE PATH "" FORCE)
elseif(APPLE)
    set(SDL_LIBRARY_DIRS "${SDL_ROOT_DIR}/lib/OSX" CACHE PATH "" FORCE)
endif()
set(SDL_LIBRARIES SDL2 SDL2main CACHE PATH "" FORCE)
set(SDL_INCLUDE_DIRS ${SDL_HEADER_DIR} CACHE PATH "" FORCE)
set(SDL_RUNTIMELIBS ${SDL_DLLS} CACHE PATH "" FORCE)