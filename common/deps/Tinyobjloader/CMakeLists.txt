set(TINYOBJ_ROOT_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE PATH "" FORCE)
set(TINYOBJ_HEADER_DIR ${TINYOBJ_ROOT_DIR}/include)

include_directories(${TINYOBJ_HEADER_DIR})

set(TINYOBJ_INCLUDE_DIRS ${TINYOBJ_HEADER_DIR} CACHE PATH "" FORCE)
